// TODO: Implement bridge client
// This will hold the agent-facing API client that connects
// to the bridge routes for reading/writing document state.

export { PACKAGE_NAME as CORE_PACKAGE } from "@proof-clone/core";

export const PACKAGE_NAME = "@proof-clone/agent-bridge";
