{
  "name": "@proof-clone/agent-bridge",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@proof-clone/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
